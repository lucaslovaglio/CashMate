package com.cashmate.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// Colores principales
val Green40 = Color(0xFF01D680) // Verde fuerte (principal)
val Green80 = Color(0xFF00B268) // Verde más oscuro
val Green20 = Color(0xFFB3EFCB) // Verde pastel claro

// Tonos secundarios
val GreenGrey40 = Color(0xFFC2E0D7) // Verde gris oscuro (para textos secundarios)
val GreenGrey80 = Color(0xFF2A3D35) // Verde gris profundo (para fondos oscuros)
val GreenGrey20 = Color(0xFFDDEAE6) // Verde gris claro (para fondos claros)

// Colores terciarios
val Mint40 = Color(0xFF81F2C0) // Menta suave (para detalles/accentos)
val Mint80 = Color(0xFF60C9A0) // Menta más saturada
val Mint20 = Color(0xFFDFFEF1) // Menta claro (fondos o tarjetas)

// Fondos
val BackgroundDark = Color(0xFF121212) // Fondo oscuro estándar
val BackgroundLight = Color(0xFFF5F5F5) // Fondo claro estándar

// Superficies (como tarjetas)
val CardDark = Color(0xFF1E1E1E) // Fondo de tarjeta en tema oscuro
val CardLight = Color(0xFFFFFFFF) // Fondo de tarjeta en tema claro

// Colores de texto
val TextPrimaryDark = Color(0xFFE0E0E0) // Texto principal en tema oscuro
val TextPrimaryLight = Color(0xFF212121) // Texto principal en tema claro
val TextSecondaryDark = Color(0xFFB0B0B0) // Texto secundario en tema oscuro
val TextSecondaryLight = Color(0xFF757575) // Texto secundario en tema claro

// Alertas o errores
val Error = Color(0xFFD32F2F) // Rojo para errores
val Warning = Color(0xFFFFA726) // Naranja para advertencias
val Success = Color(0xFF388E3C) // Verde oscuro para éxito

